{
  "name": "react-notifications-context",
  "version": "0.0.2",
  "private": false,
  "author": "Simon Emmott <simon.emmott@yahoo.co.uk>",
  "keywords": [
    "react",
    "notifications",
    "context",
    "alerts"
  ],
  "babel": {
    "presets": [
      "@babel/preset-react"
    ]
  },
  "main": "dist/Notifications.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/simonemmott/k2-tools-react-notifications-context.git"
  },
  "dependencies": {
    "@k2_tools/utils": "^1.0.2",
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/user-event": "^14.4.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "stop": "`lsof -i :3000 | grep node | grep LISTEN | awk '{print \"kill -9 \"$2}'`",
    "build": "rm -rf dist && mkdir dist &&  babel src/component -d dist --copy-files",
    "test": "react-scripts test",
    "test:coverage": "react-scripts test --coverage",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/cli": "^7.18.10",
    "@babel/preset-react": "^7.18.6",
    "@testing-library/react": "^13.3.0",
    "jest-cli": "^28.1.3",
    "react-test-renderer": "^18.2.0"
  }
}
